<% if (title) { %>
    <h1 class="management-title"><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>

<%- messages() %>


<form class="management-form" action="/inv/update" method="post">
  <h2>Edit Vehicle</h2>
  <div>
    <label for="inv_make">Make:</label>
    <input type="text" id="inv_make" name="inv_make" value="<%= inv_make %>" required />
  </div>

  <div>
    <label for="inv_model">Model:</label>
    <input type="text" id="inv_model" name="inv_model" value="<%= inv_model %>" required />
  </div>

  <div>
    <label for="inv_year">Year:</label>
    <input type="number" id="inv_year" name="inv_year" value="<%= inv_year %>" required />
  </div>

  <div>
    <label for="inv_description">Description:</label>
    <textarea id="inv_description" name="inv_description" required><%= inv_description %></textarea>
  </div>

  <div>
    <label for="inv_price">Price:</label>
    <input type="number" id="inv_price" name="inv_price" value="<%= inv_price %>" required />
  </div>

  <div>
    <label for="inv_miles">Miles:</label>
    <input type="number" id="inv_miles" name="inv_miles" value="<%= inv_miles %>" required />
  </div>

  <div>
    <label for="inv_color">Color:</label>
    <input type="text" id="inv_color" name="inv_color" value="<%= inv_color %>" required />
  </div>
  <div>
    <label for="classification_id">Classification:</label>
      <!-- Assuming `classificationList` is passed as options -->
      <%- classificationSelect %>
  </div>
  <div>
    <label for="inv_image">Image:</label>
    <input type="file" id="inv_image" name="inv_image" accept="image/*">
  </div>
  <div>
    <label for="inv_thumbnail">Thumbnail:</label>
    <input type="file" id="inv_thumbnail" name="inv_thumbnail" accept="image/*">
  </div>

  <button type="submit" name="submit" value="Update Vehicle" >Update Vehicle</button>
</form>

<%- messages() %>


<% if (errors) { %>
  <ul class="notice">
 <% errors.array().forEach(error => { %>
   <li><%= error.msg %></li>
<%  }) %>
 </ul>
<% } %>
